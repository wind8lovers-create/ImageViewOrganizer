# ImageViewOrganizer 操作マニュアル（最新版）

対象アプリ: `ImageViewOrganizer` (Kotlin + Jetpack Compose / Android 12+ / タブレット・Pixel 9 想定)

---

## 1. 概要・主な特徴

`ImageViewOrganizer` は、フォルダ内や ZIP 書庫内の画像を高速に一覧表示し、**「重複・類似画像の比較」「手動グルーピング」「連番リネーム・仕分け移動」** をスマートに行うことができる画像整理アプリです。

| 主な機能 | 概要 |
|---|---|
| **高速一覧・全画面表示** | 大量画像でも段階的ストリーミングで即座に表示。ピンチズームやダブルタップ拡大に対応 |
| **拡張選択 (類似・重複検索)** | 知覚ハッシュ(dHash)や彩度・明度・RGB5色により、似た画像・重複画像を枠色別で自動抽出 |
| **安心の自動フォールバック** | 条件に合う画像が0件でも作業が止まらないよう、自動でハッシュOFFやソート切替を実行 |
| **スマート中断** | ハッシュ計算中に解除やフォルダ移動を行っても、裏の処理が即座にピタッと安全停止 |
| **ピンクのタグ (ラベル機能)** | **通常タップでラベル選択**、**長押しでサブフォルダ移動**、**「..⤴」で親フォルダ復帰** |
| **手動分類 (グルーピング)** | A〜Zのカテゴリごとにテーマ別グループを作成し、島状タイルで整理 |
| **安全なファイル操作** | 移動・コピー・削除・連番リネーム前に必ずシステムの確認ダイアログが表示される安全設計 |

---

## 2. 画面構成と上部メニューバー

下部バーはすべて廃止され、**画面上部の一体型メニューバー（TopBar）** にすべての機能が集約されています。

```
上部メニューバー（TopBar）
├─ 【左側: フォルダ・履歴】
│   ├─ 📁 フォルダを開く … SAFピッカーで任意のフォルダを選択
│   ├─ 🗜 ZIPを開く      … .zip書庫を選択（一時展開して閲覧）
│   ├─ 🕓 履歴          … 既定のフォルダ(Download/未整理)や過去のフォルダ履歴
│   └─ フォルダ名表示    … タップでフルパス展開表示、再タップで格納
│
├─ 【中央: 整理・仕分け・ラベル】
│   ├─ 🏷️ ソートボタン  … 並び替えボトムシートを表示（11種類の並び順）
│   ├─ ★ 拡張選択       … 類似・重複画像の絞り込みパネルを展開（詳細は第3章）
│   ├─ 🏷️ (ピンク #FF67C6) ラベル選択 …
│   │    ├─ 【通常タップ】 作業ラベルの選択・切り替え
│   │    ├─ 【長押し】     直下の同名フォルダへ移動（存在する場合）
│   │    └─ 【..⤴】       一階層上の親フォルダへ戻る
│   ├─ 【選択中のみ表示される操作ボタン】
│   │    ├─ 📁 連番移動 … 選択画像を「[ラベル]」フォルダへ連番移動
│   │    ├─ 🏷️ 連番リネーム … 移動せず、現在のフォルダ内で「[ラベル]連番」にリネーム
│   │    ├─ 📋 コピー   … 別フォルダを選択してコピー
│   │    ├─ 🚚 移動     … 別フォルダを選択して移動
│   │    ├─ 🗑️ 削除     … 選択画像を削除（確認ダイアログあり）
│   │    └─ ✕ 選択解除  … 選択をすべてクリア
│
└─ 【右側: 閲覧・補助】
    ├─ 🔖 しおり機能    … タップで選択中画像へ順次ジャンプ、長押しで最初の選択画像へ戻る
    ├─ ▶ スライドショー … 現在の一覧を全画面で自動再生
    ├─ 🖼 サムネイル切替 … グリッドの表示サイズを切り替え（小:100dp / 大:150dp）
    └─ 🗂️ 分類一覧切替  … 「画像一覧」⇔「手動分類グループ一覧」の画面切り替え
```

---

## 3. 拡張選択（★）機能・重複類似画像検索

上部メニューの **「★」アイコン** をタップすると、拡張選択モードが起動し、専用のコントロールバーが表示されます。

### 3-1. 拡張選択バーの構成
```
拡張選択バー（展開時）
├─ 下層📁:OFF / ON … サブフォルダも含めて検索するかどうかの切り替え
├─ ハッシュ値 スイッチ … ONで知覚ハッシュ比較を実行（閾値スライダー 1〜17、−/＋微調整ボタン）
├─ RGB5色 スライダー … 代表色パレットの類似度で判定（0%〜100%）
├─ 彩度 / 明度 スライダー … 基準画像との色味差を許容範囲で絞り込み（0.0〜0.35）
└─ 縦横比 スイッチ   … アスペクト比が一致する画像のみに絞り込み
```

### 3-2. 進化した親切設計（最新機能）
- **未選択でも全体検索可能**: 画像を選択せずに「★」を押すと、フォルダ全体のすべての画像から似ているペアを自動抽出します（選択中ならその画像が基準になります）。
- **ハッシュ値該当なし時の自動オフ**:
  ハッシュ値比較で該当する画像が0組だった場合、**「該当する画像はありません」** とメッセージを表示し、自動的に **ハッシュ値スイッチをOFF** に切り替えて全画像を表示します（作業が止まる画面になりません）。
- **スマート即時中断（キャンセル対応）**:
  全何枚中何枚という読み込み処理の最中でも、**「★」をもう一度押して解除** したり、**ラベルからフォルダ移動** したりした瞬間に、裏の計算が即座にストップし、進捗表示もスッと消えます。

---

## 4. ソート（並び替え）と自動フォールバック

ソートボタンをタップすると、以下の11種類の並び順を選択できます。

| ソート項目 | 動作・特徴 |
|---|---|
| **🏷️ グループ連番（枠色別）↑** | `_nn_mm` 形式のファイルを抽出し、グループごとにカラー枠線をつけて表示 |
| **ファイル名 ↑ / ↓** | ファイル名（昇順 / 降順） |
| **サイズ ↑ / ↓** | ファイルサイズ（昇順 / 降順） |
| **日付 ↑ / ↓** | 更新日時（デフォルトは 日付 ↓: 新しい順） |
| **撮影日 ↑ / ↓** | 写真のEXIF撮影日時（選択時のみEXIFを遅延読み込み） |
| **形式 ↑ / ↓** | 拡張子別（.jpg, .png 等） |

> 💡 **自動フォールバック機能**  
> 「🏷️ グループ連番」を選んだ際、対象形式の画像が1枚もない場合は、自動的に **「日付 ↑（古い順）」** に切り替わって全画像を表示します。「画像が見つかりませんでした」画面で作業が中断されることはありません。

---

## 5. ラベル機能とフォルダ階層移動

上部中央の **ピンクのタグアイコン（`#FF67C6`）** をタップすると、ラベルドロップダウンメニューが開きます。

### 5-1. 操作方法
- **【通常タップ】 ラベル選択**:
  作業用のラベル（例: `01犬`, `02猫` など）を選択します。選択されたラベルは上部に表示され、連番移動やリネームの基準名になります。
- **【長押し】 サブフォルダ移動**:
  現在のフォルダ直下にそのラベルと同名のフォルダが存在する場合、**そのサブフォルダへ瞬時に移動** して中身を読み込みます（フォルダがない場合は案内文が表示されます）。
- **【最上部の「..⤴」】 親フォルダ復帰**:
  サブフォルダに潜っているときにタップすると、直前の親フォルダへスムーズに戻ります。

---

## 6. 一覧表示・選択・ファイル操作

### 6-1. 一覧グリッドの操作
- **タップ**: その画像を全画面ビューアで表示します。
  - 左右スワイプで前後の画像へ移動
  - ピンチイン・アウト、ダブルタップで拡大縮小
  - ✕ボタンまたは長押しで一覧に戻る
- **長押し**: 複数選択モードを開始します。
  - 1枚選択中に別の画像を長押しすると、その間にある画像を **一括範囲選択** できます。
  - 選択モード中は、タップするだけで選択のON/OFFが切り替わります。

### 6-2. 選択中のアクション
画像を1枚以上選択すると、上部バーに以下の実行ボタンが現れます。
- **連番移動（連番→📁[ラベル]）**: 選択された画像を「[ラベル]」サブフォルダへ、`[ラベル]_[グループ番号]_[画像番号].[拡張子]`（例: `01犬_00_00.jpg`）の形式で連番リネームして移動します。
- **連番リネーム（Rename→[ラベル]連番）**: フォルダ移動は行わず、現在のフォルダ内でラベル付きの連番名にリネームします。
- **コピー / 移動**: 保存先・移動先のフォルダピッカーが開き、選んだ場所へまとめて転送します。
- **削除**: 選択画像を端末から削除します（必ず確認ダイアログが表示されます）。

---

## 7. 手動分類（グルーピング）

上部右側の **「分類一覧」** ボタンを押すと、手動分類画面へ切り替わります。

### 7-1. 分類一覧画面
- **A〜Z のカテゴリ** ごとにグループが島状のタイルで整列表示されます。
- タイルには代表サムネイル画像とグループ名（枚数）が表示されます。
- **タイルをタップ**: そのグループの個別画面（GroupDetailScreen）に入ります。
- **タイルを長押し**: そのカテゴリ全体の画像を通しでスライドショー再生します。
- **グループ名を長押し**: グループ名の変更ダイアログを開きます。

### 7-2. 新規グループ作成（画像一覧から）
1. 画像一覧で、まとめたい画像を複数長押し選択します。
2. 上部右側のチップが **「分類登録」** に変わるのでタップします。
3. カテゴリ（A〜Z）とグループ名を入力してOKを押すと、新規グループが作成されます。

---

## 8. よくある質問・トラブルシューティング

| 症状 / 疑問 | 原因と解決方法 |
|---|---|
| **「画像が見つかりませんでした」と出る** | 本当にフォルダ内に画像が0枚のときのみ表示されます。端末のファイルマネージャーで直接コピーした画像は未登録の場合があるため、「フォルダを選択する」から直接フォルダを指定してください。 |
| **ソートやフィルタで空画面になって作業が止まることはある？** | ありません。フォルダに画像がある場合は、条件不一致でも「該当する画像がありません」と薄く表示されるか、自動で通常一覧に切り替わります。 |
| **ハッシュ計算が長くて途中でやめたい** | 「★」をもう一度タップして解除するか、別のフォルダへ移動してください。裏の計算処理が即座に安全停止します。 |
| **ラベルをタップしたのにフォルダ移動しない** | 通常タップは「作業ラベルの選択」です。フォルダ移動したいときは **ラベルを長押し** してください。 |
| **親フォルダに戻る「..⤴」が押せない** | 起点となる一番上のフォルダにいる時は薄いグレー表示になり無効化されます。サブフォルダへ潜るとハッキリ表示されて押せるようになります。 |
| **誤って削除ボタンを押してしまわないか心配** | 削除や移動の実行前には、必ずAndroidシステムの確認ダイアログが表示されます。「許可」を押さない限り実行されません。 |

---

*最新更新: 2026年9月 / ImageViewOrganizer プロジェクト最新仕様準拠*
