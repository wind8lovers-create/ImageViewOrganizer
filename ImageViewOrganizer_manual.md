# ImageViewOrganizer 操作マニュアル

対象アプリ: `ImageViewOrganizer`(Kotlin + Jetpack Compose / タブレット・Pixel 9 想定)

---

## 1. 概要・できること

ImageViewOrganizer は、フォルダ内(または ZIP 内)の画像を一覧表示し、以下のことができる個人用の画像整理アプリです。

| できること | 概要 |
|---|---|
| 一覧・全画面表示 | フォルダ/ZIP内の画像をグリッド表示し、タップで全画面拡大 |
| 同一画像の検出 | 知覚ハッシュ(dHash)で似た画像を枠色でグルーピング表示 |
| 拡張選択(絞り込み) | 彩度・明度・ハッシュ値・縦横比・代表色(RGB5色)で類似画像だけに絞り込み |
| 並び替え | ファイル名・サイズ・日付・撮影日・形式の10項目 |
| 選択操作 | 長押しで選択モード開始 → 移動・ZIP化・削除・連番リネーム |
| 手動分類(グルーピング) | A〜Zのカテゴリ+連番でグループを作り、画像をまとめて管理 |
| スライドショー | 通常一覧/カテゴリ単位/グループ単位で自動再生 |

> 💡 移動・削除・リネームなどのファイル操作は、実行前に必ずシステムの同意ダイアログが表示される「安全第一」設計です。誤操作してもいきなり消えることはありません。

---

## 2. 初期設定

1. **初回起動時の権限許可**
   起動すると自動的に画像アクセス権限(`READ_MEDIA_IMAGES`)のリクエストが出ます。「許可」を選んでください。
2. **既定の読込フォルダ**
   起動時は自動的に `Pictures/未整理` フォルダを読み込もうとします。画像が見つからない場合は「画像が見つかりませんでした」という案内画面が表示されます。
3. **フォルダ/ZIPを開く**
   1. 上部バー左側の 📁(フォルダを開く)または 🗜(ZIPを開く)アイコンをタップ
   2. 端末標準のピッカーで対象のフォルダ、または `.zip` ファイルを選択
   3. 選択すると自動的に読み込みが始まる(大量枚数の場合は新しい順に段階的に表示される)
4. **履歴から開く**
   上部バーの 🕓(履歴)アイコン → ドロップダウンから「既定のフォルダ」または過去に開いたフォルダ/ZIPを選択

> ⚠️ ファイルマネージャーで直接コピーした画像は端末の写真データベースに未登録の場合があり、一覧に出てこないことがあります。その場合は「フォルダを選択する」ボタンから直接フォルダを指定してください。

---

## 3. 基本的な使い方・主要機能

### 3-1. 上部バー(通常時)のコマンドツリー

```
上部バー
├─ 📁 フォルダを開く … SAFピッカーでフォルダ選択 → 読込
├─ 🗜 ZIPを開く      … .zipファイルを選択 → 展開して読込
├─ 🕓 履歴
│   ├─ 既定のフォルダ(Download/未整理)
│   └─ 過去に開いたフォルダ/ZIP 一覧(タップで再読込)
├─ フォルダ名表示(タップでフルパス/ファイル名を表示・再タップで非表示)
├─ 🖼/🖽 サムネイルサイズ切替 … 100dp ⇔ 150dp をトグル
├─ [拡張選択] チップ … 詳細は 3-2 参照(押す前に画像を1枚以上選択しておく必要あり)
├─ ▶/⏹ スライドショー … 現在の一覧をそのまま自動再生
└─ [分類一覧]⇔[画像一覧] チップ … 画面モード切替(状況により表示・挙動が変化、詳細は 3-4 参照)
```

### 3-2. 拡張選択パネル(似た画像の絞り込み)

**前提条件:** あらかじめ長押しで1枚以上選択している状態で「拡張選択」をタップすること。未選択の場合は「まず、似た画像を探したい画像を選択してください」という案内が出るだけで開きません。

```
[拡張選択] チップ ON
├─ 彩度スライダー(0〜35%) … 基準画像との彩度差の許容範囲
├─ 明度スライダー(0〜35%) … 基準画像との明度差の許容範囲
├─ プリセットチップ(OFF→A→B→C→OFF循環)
│   ├─ A: 彩度15% / 明度0%(彩度のみで判定)
│   ├─ B: 彩度0% / 明度30%(明度のみで判定)
│   └─ C: 彩度30% / 明度0%(彩度のみ、より広めに判定)
├─ ハッシュ値スイッチ … ON で知覚ハッシュが完全一致する画像のみに絞る
├─ 縦横比スイッチ … ON で基準画像とアスペクト比が同じ画像のみに絞る
└─ RGB5色スライダー(0〜100%) … 代表色パレットの類似度で絞り込み(0%=絞り込みなし)
```

もう一度「拡張選択」チップを押すと解除され、通常表示に戻ります(絞り込み条件もリセット)。

### 3-3. 一覧・選択・ファイル操作(GALLERY画面)

```
画像一覧(グリッド)
├─ タップ … 全画面表示(FullscreenViewer)を開く
│    └─ 全画面表示中
│         ├─ 横スワイプ … 前後の画像に移動
│         ├─ ピンチ/ダブルタップ … 拡大縮小
│         └─ 長押し / ✕ボタン … 一覧に戻る
└─ 長押し … 選択モード開始
     ├─ 1枚だけ選択中に別画像を長押し … アンカーから対象までを範囲選択
     └─ 選択モード中のタップ … その画像の選択ON/OFFを切替

下部バー(通常時)
├─ 並び替え(現在のソート名表示) … ソートのボトムシートを開く(3-3-1参照)
├─ 選択 … ボタン自体は案内のみ(実際の選択開始は長押しで行う)
└─ リネーム … ボタン自体は案内のみ(通常時は無効。選択モードで使う)

下部バー(選択モード中)
├─ 移動 … 選択画像を _Moved_ フォルダへ移動
├─ ZIP … 選択画像を「年月日時分_連番.zip」として書き出し
├─ 削除 … 選択画像を削除(同意ダイアログあり)
├─ RN(リネーム) … 表示順に連番リネーム(3-3-2参照)
├─ 選択へ … タップで選択中の画像の位置へ順番にジャンプ、長押しで先頭へ戻る
└─ 解除(件数) … 選択モードを終了し、選択をクリア
```

#### 3-3-1. 並び替え(ソート)一覧

| 項目 | 昇順 | 降順 |
|---|---|---|
| ファイル名 | ↑ | ↓ |
| サイズ | ↑ | ↓ |
| 日付(更新日時) | ↑ | ↓ |
| 撮影日(EXIF) | ↑ | ↓ |
| 形式(拡張子) | ↑ | ↓ |

- 既定値は「日付 ↓(新しい順)」
- 「撮影日」でのソートを選んだ時だけ、その場でEXIF読込が走る(初期表示を軽くするための遅延読込)

#### 3-3-2. 連番リネームの命名規則

```
REN + 年月日時分(10桁) + "_" + 連番 + 拡張子
例: REN2608041228_01.jpg

連番の進み方:
  01 〜 99 … 数字2桁
  AA 〜 ZZ … 数字を使い切ったらアルファベット2桁(676通り)
  それ以上 … "ZZ" + 追加の数字で衝突回避
```

拡張子は元のファイルのものを維持(再圧縮による画質劣化を避けるため)。

#### 3-3-3. スライドショー(通常一覧)

```
▶ スライドショー開始
├─ 秒数プルダウン … 0.3秒 / 0.5秒 / 0.8秒 / 2.0秒 / 5.0秒
├─ ⏸/▶ 一時停止・再生切替
│    └─ 一時停止中のみ ピンチズーム・パン・ダブルタップ拡大が可能
├─ 横スワイプ … 手動でページ送り
└─ 終了 … 一覧に戻り、最後に見ていた画像位置までスクロール
```

### 3-4. 手動分類(グルーピング)機能

上部バー右側のチップは状況によって役割が変わる共用ボタンです。

```
チップの表示・役割(状況別)
├─ 通常時(画像一覧・非選択) … 表示「分類一覧」 → 押すと分類一覧画面へ切替
├─ 分類一覧を見ている時 … 表示「画像一覧」 → 押すと画像一覧に戻る
├─ 画像一覧で選択モード中 … 表示「分類登録」 → 押すと新規グループ作成ダイアログを開く
└─ 画像追加モード中 … 表示「追加確定」 → 押すと選択画像を対象グループへ追加確定
```

#### 3-4-1. 分類一覧画面(CLASSIFICATION_LIST)

```
分類一覧(グリッド、カテゴリごとに島状に整列)
├─ タイル タップ … そのグループの中に入る(グループ内画面へ)
├─ タイル(代表画像) 長押し … そのカテゴリ全体を通しでスライドショー再生
└─ タイル下部の名前ラベル 長押し … グループ名の変更ダイアログを開く
```

タイルは「代表画像 + カテゴリ名(枚数)」で構成され、カテゴリごとに枠色が変わります。

#### 3-4-2. 新規グループ作成/グループ名変更ダイアログ

新規作成(「分類登録」)とリネーム(名前ラベル長押し)は同じダイアログを共用します。

```
ダイアログ
├─ カテゴリ プルダウン(A〜Z)
│    └─ 選ぶと「次の連番:既存テーマ名」がプレビュー表示される(例: C_04:神社)
├─ 名前 テキスト入力(自由編集、空欄可)
│    └─ 空欄のままOK … 連番名のみ(例: "C_04")が採用される
├─ OK … 確定して反映
│    └─ 新規作成時: 既に他グループに入っている画像は自動で除外(1画像1グループ原則)
└─ キャンセル … 何も変更せず閉じる
```

#### 3-4-3. グループ内画面(GROUP_DETAIL)

```
グループ内画面
├─ 左上 ← … 分類一覧に戻る
├─ 並び替え(Sort) … このグループ専用のソートボトムシートを開く(項目は3-3-1と同じ10種)
├─ ▶ スライドショー … このグループの画像だけを再生
├─ 左右スワイプ(画面全体) … 同じカテゴリ内の隣のグループへ切替(例: C_01 ⇔ C_02)
├─ 画像タップ … 全画面表示
├─ 画像長押し / 選択中のタップ … 「画像削除」対象として選択(グループから外すだけ、端末のファイルは消えない)
└─ 下部バー
     ├─ サムネ指定(★) … ちょうど1枚選択中のみ表示。分類一覧での代表画像に設定
     ├─ 画像追加 … 画像一覧へ切替、同カテゴリの画像だけ選べる「画像追加モード」に入る
     └─ 画像削除(件数) … 1枚以上選択中のみ表示。グループから除外(全部外すとグループごと消える)
```

#### 3-4-4. 画像追加モード

```
「画像追加」をタップ
├─ 画面が画像一覧(GALLERY)に切替わる
├─ 既に他グループに属する画像は暗く表示され選択不可
│    └─ ただし対象と同じカテゴリの画像は明るいまま選択可能(グループ統合用)
├─ 画像をタップして複数選択
├─ 上部バーの「追加確定」チップ … 選択画像を対象グループへ追加してグループ内画面に戻る
└─ 下部バーの「キャンセル」 … 何も追加せずグループ内画面に戻る
```

---

## 4. よくあるトラブルと対処法

| 症状 | 原因・対処法 |
|---|---|
| 起動しても画像が1枚も出てこない | ファイルマネージャーで直接コピーした画像は端末の写真DBに未登録の場合がある。「フォルダを選択する」から対象フォルダを直接指定する |
| 「拡張選択」を押しても何も起きない | 拡張選択は「先に画像を1枚以上選択」してから使う機能。まず長押しで選択モードに入り、対象画像を選んでから押す |
| 下部バーの「移動/ZIP/削除/RN」がグレーアウトして押せない | 読込中(ストリーミング中)またはグルーピング計算中は無効化される仕様。「読込中…」表示が消えるまで待つ |
| 「分類登録」で選んだはずの画像が反映されない | 既に別のグループに入っている画像は自動的に対象から除外される(1画像1グループ原則)ため。分類一覧で現在の所属先を確認する |
| グループ内画面で「画像削除」を押しても端末からファイルが消えない | 仕様通りの挙動。この操作は「グループから外す」だけで、実ファイルの削除ではない。実ファイルを消したい場合は通常の画像一覧側で選択→削除を行う |
| スワイプでグループを切り替えたら表示がおかしい/一瞬だけプレビューになる | 隣のグループのページはスワイプ確定前は軽量プレビュー(代表画像+名前のみ)。スワイプを最後まで完了させると通常のグループ内画面に切り替わる仕様 |
| 移動・削除・リネームの直前に確認ダイアログが出る | Android標準のメディア変更同意ダイアログ。誤操作防止のための仕様なので「許可」を選べば処理が続行される |
| 「サムネ指定」ボタンが出てこない | ちょうど1枚だけ選択している時だけ表示される仕様。2枚以上/0枚選択中は表示されない |
| リネーム後のファイル名の連番が急にアルファベットになった | 仕様通り。01〜99を使い切ると自動的にAA〜ZZ(676通り)に移行する命名規則 |

---

*このマニュアルは `ImageViewOrganizer` プロジェクト内のソースコード(TopBar / BottomBar / MainScreen / GroupDetailScreen / ClassificationListScreen / RenameUtil 等)を基に作成しています。*
