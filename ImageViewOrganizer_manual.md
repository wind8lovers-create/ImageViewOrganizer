# ImageViewOrganizer 操作マニュアル（最新完全版）

対象アプリ: `ImageViewOrganizer` (Kotlin + Jetpack Compose / Android 12+ / タブレット・Pixel 9 想定)

---

## 1. 概要・主な特徴

`ImageViewOrganizer` は、フォルダ内や ZIP 書庫内の大量画像を高速に一覧表示し、**「重複・類似画像の比較」「グループ連番比較としおり巡回」「手動グルーピング」「連番リネーム・仕分け移動」** をスマートかつ直感的に行える高機能画像整理アプリです。

| 主な機能 | 概要 |
|---|---|
| **高速一覧・全画面表示** | 大量画像でも段階的ストリーミングで即座に表示。ピンチズームやダブルタップ拡大に対応 |
| **新・選択モード（カテゴリータップ式）** | 上部のカテゴリーラベルをタップするだけで「0枚選択」から選択モード開始。長押し事故を防止 |
| **選択画像の光度40%ダウン** | チェックを付けた画像はシックに40%暗くなり、白い丸チェックアイコンがくっきり際立つ高視認性UI |
| **拡張選択 (類似・重複検索)** | 知覚ハッシュ(dHash)や彩度・明度・RGB5色により、似た画像・重複画像を枠色別で自動抽出 |
| **グループ表示 ＆ しおり巡回連携** | ★比較で重複した枠の画像を長押しするとグループ一覧へ展開。**🔖しおりボタンで重複画像間を順次ジャンプ** |
| **安全なファイル操作 ＆ 自動バックアップ同期** | グループ比較中に画像を削除・移動しても、裏のハッシュ画面と自動同期して不整合を完全防止 |
| **ピンクのタグ (ラベル機能)** | **通常タップでラベル選択**、**長押しでサブフォルダ移動**、**「..⤴」で親フォルダ復帰** |
| **安心の自動フォールバック** | 条件に合う画像が0件でも作業が止まらないよう、自動でハッシュOFFやソート切替を実行 |
| **スマート即時中断** | ハッシュ計算中に解除やフォルダ移動を行っても、裏の重い処理が即座にピタッと安全停止 |

---

## 2. 画面構成と上部メニューバー

下部バーはすべて廃止され、**画面上部の一体型メニューバー（TopBar）** にすべての機能が集約されています。

```
上部メニューバー（TopBar）
├─ 【左側: フォルダ・履歴】
│   ├─ 📁 フォルダを開く … SAFピッカーで任意のフォルダを選択
│   ├─ 🗜 ZIPを開く      … .zip書庫を選択（一時展開して閲覧）
│   ├─ 🕓 履歴          … 既定のフォルダ(Download/未整理)や過去のフォルダ履歴
│   ├─ ≡ ソートボタン   … ドロップダウンで並び順を選択（11種類の並び順）
│   └─ ★ 拡張選択       … 類似・重複画像の絞り込みパネルを展開（詳細は第4章）
│
├─ 【中央: 整理・仕分け・ラベル】
│   ├─ -[ラベル名]- カテゴリーラベル表示部（タップ/長押し対応）
│   │    ├─ 【タップ】     選択モードの ON / OFF 切り替え（0枚選択スタート）
│   │    │                 ・通常時: 白文字
│   │    │                 ・選択モード中: ピンク文字（#FF67C6）
│   │    │                 ・グループ比較中: ライトグリーン文字（#4FFF94）
│   │    └─ 【長押し】     グループ比較表示から、元の★ハッシュ比較一覧へ一瞬で復帰
│   │
│   ├─ 【選択中のみ表示】〇枚選択ボタン（タップ/長押し対応）
│   │    ├─ 【タップ】     連番移動・リネーム・コピー・移動・削除のプルダウンメニュー
│   │    └─ 【長押し】     選択モードを維持したまま、チェックだけ全解除（0枚選択にリセット）
│   │
│   └─ 🏷️ (ピンク #FF67C6) ラベル選択プルダウン
│        ├─ 【通常タップ】 作業ラベルの選択・切り替え
│        ├─ 【長押し】     直下の同名フォルダへ移動（存在する場合）
│        └─ 【..⤴】       一階層上の親フォルダへ戻る
│
└─ 【右側: 閲覧・補助】
    ├─ 🔖 しおり機能    … 選択画像（または記憶した類似画像）へ順次ジャンプ、長押しで最初の画像へ
    ├─ ▶ スライドショー … 現在の一覧を全画面で自動再生（再生間隔表示付き）
    └─ ⊞ グリッド切替   … サムネイルの表示サイズを切り替え（小:100dp / 大:150dp）
```

---

## 3. 基本操作と選択モード（新仕様）

### 3-1. 一覧グリッドの操作
- **通常タップ**: その画像を全画面ビューアで表示します。
  - 左右スワイプで前後の画像へ移動
  - ピンチイン・アウト、ダブルタップで拡大縮小
  - ✕ボタンまたは長押しで一覧に戻る
- **通常時の画像長押し**:
  - 画像長押しによる誤った選択突入は廃止されました。
  - ★拡張選択（ハッシュ比較）中の長押しは、専用の **「グループ比較表示」** のトリガーとして機能します（詳細は第5章）。

### 3-2. 選択モードの開始と終了（カテゴリータップ式）
- **選択モードに入る**:
  - 上部中央の **カテゴリーラベル（`-[ラベル名]-`）を1回タップ** します。
  - ラベルの文字色が **鮮やかなピンク色（`#FF67C6`）** に変わり、**「0枚選択」** の状態で選択モードが開始されます。
- **画像の選択**:
  - 画像をタップするだけで、1枚ずつチェック（選択）の ON / OFF が切り替わります。
- **選択画像のシックな暗表示（光度40%ダウン）**:
  - チェックが入った画像は、**光度（明るさ）が40%暗く表示** され、右上の白い丸チェックマークがはっきりと際立ちます。一目でどれを選んだかが直感的に分かります。
- **「〇枚選択」ボタンの長押し（選択モード維持リセット）**:
  - チェックを最初から選び直したいときは、「〇枚選択」ボタンを長押しします。
  - 選択モード（ピンク色）はそのまま維持され、チェックだけが綺麗に外れて **「0枚選択」** に戻ります。
- **選択モードを終了する**:
  - 上部の **カテゴリーラベルをもう一度タップ** します。
  - 文字色が白に戻り、すべてのチェックが解除されて通常の一覧表示に戻ります。

---

## 4. 拡張選択（★）機能・重複類似画像検索

上部メニューの **「★」アイコン** をタップすると、拡張選択モードが起動し、重複・類似画像の比較パネルが表示されます。

### 4-1. 拡張選択パネルの構成
```
拡張選択パネル（展開時）
├─ 下層📁:OFF / ON … サブフォルダも含めて検索するかどうかの切り替え
├─ ハッシュ値 スイッチ … ONで知覚ハッシュ比較を実行（閾値スライダー 1〜17、−/＋微調整ボタン）
├─ RGB5色 スライダー … 代表色パレットの類似度で判定（0%〜100%）
├─ 彩度 / 明度 スライダー … 基準画像との色味差を許容範囲で絞り込み（0.0〜0.35）
└─ 縦横比 スイッチ   … アスペクト比が一致する画像のみに絞り込み
```

### 4-2. 進化した親切設計
- **未選択でも全体検索可能**: 画像を選択せずに「★」を押すと、フォルダ全体の全画像から似ているペアを自動抽出します（選択中ならその画像が基準になります）。
- **ハッシュ値該当なし時の自動オフ**:
  ハッシュ値比較で該当する画像が0組だった場合、**「該当する画像はありません」** とメッセージを表示し、自動的に **ハッシュ値スイッチをOFF** に切り替えて全画像を表示します（作業が止まる画面になりません）。
- **スマート即時中断（キャンセル対応）**:
  ハッシュ計算の最中でも、**「★」をもう一度押して解除** したり、**フォルダ移動** したりした瞬間に、裏の計算が即座にストップし、進捗表示もスッと消えます。

---

## 5. グループ表示 ＆ しおり巡回連携（最重要ハイライト）

★ハッシュ比較で見つかった重複画像たちを、前後の連番シリーズ全体と見比べながら整理する超強力な機能です。

### 5-1. 一連の操作フロー
1. **「★」アイコンをタップ** してハッシュ比較を実行します。
   - 似ている画像同士がカラー枠（例: 赤枠 `[ ⓵ ⓶ ⓷ ]`）で囲まれて隣り合って並びます。
2. **枠内の画像を「長押し」** します。
   - アプリがその枠内にいる画像たち（未リネーム画像も含む3枚など）を記憶します。
   - 瞬時に **「🏷️ グループ連番（枠色別）」の全体一覧** に切り替わり、記憶した画像たち（3枚）にチェックが付いて **光度40%オフ（暗く）** 表示されます。
   - 自動的に **1枚目の画像の位置へ画面がスクロール** します。
   - 上部のカテゴリーラベルが **鮮やかなライトグリーン（`#4FFF94`）** に変わります。
3. **上部メニューの 🔖「しおり」ボタンをタップ** します。
   - タップするたびに、グループ一覧の離れた場所にある **1枚目 ➔ 2枚目 ➔ 3枚目 ➔ 1枚目…** と画面がスクロールして順次ジャンプし、シリーズ全体を見比べることができます！
4. **整理・削除などの自由な操作**:
   - ジャンプ先で「1枚目のチェックを外す」「2枚目のグループ全体にチェックを付ける」「不要な画像を削除する」などの操作を自由に行えます。
   - 「〇枚選択」長押しでチェックを全解除（0枚）にしても、**しおりボタンはグレーアウトせず生きたまま** なので、いつでも元の3枚へピョンピョン飛んで確認できます。
5. **元のハッシュ画面へ一瞬で復帰**:
   - 上部の **カテゴリーラベル（ライトグリーン `#4FFF94`）を長押し** します。
   - 重いハッシュ再計算を待つことなく、**一瞬で元の★ハッシュ比較一覧に復帰** します！
   - しおりの位置や選択状態も綺麗サッパリ元通りにリセットされます。

### 5-2. 削除・移動時の自動バックアップ同期（完全安全設計）
グループ表示中に画像を削除または移動しても、裏で保持しているハッシュ比較のバックアップから **削除・移動された画像が自動で綺麗に取り除かれます**。
そのため、ハッシュ画面に復帰した時にも「消したはずの画像が残っている」という不整合は一切起きず、残りの重複画像たちの一覧に美しく復帰できます。

---

## 6. ソート（並び替え）と自動フォールバック

上部メニューの **≡ ソートアイコン** をタップすると、直下のプルダウンメニューから以下の11種類の並び順を即座に選択できます。

| ソート項目 | 動作・特徴 |
|---|---|
| **🏷️ グループ連番（枠色別）↑** | `_nn_mm` 形式のファイルを抽出し、グループごとにカラー枠線をつけて名前順で表示 |
| **ファイル名 ↑ / ↓** | ファイル名（昇順 / 降順） |
| **サイズ ↑ / ↓** | ファイルサイズ（昇順 / 降順） |
| **日付 ↑ / ↓** | 更新日時（デフォルトは 日付 ↓: 新しい順） |
| **撮影日 ↑ / ↓** | 写真のEXIF撮影日時（選択時のみEXIFを遅延読み込み） |
| **形式 ↑ / ↓** | 拡張子別（.jpg, .png 等） |

> 💡 **自動フォールバック機能**  
> 「🏷️ グループ連番」を選んだ際、対象形式の画像が1枚もない場合は、自動的に **「日付 ↑（古い順）」** に切り替わって全画像を表示します。「画像が見つかりませんでした」画面で作業が中断されることはありません。

---

## 7. ラベル機能とフォルダ階層移動

上部中央の **ピンクのタグアイコン（`#FF67C6`）** をタップすると、ラベルドロップダウンメニューが開きます。

### 7-1. 操作方法
- **【通常タップ】 ラベル選択**:
  作業用のラベル（例: `01犬`, `02猫` など）を選択します。選択されたラベルは上部に表示され、連番移動やリネームの基準名になります。
- **【長押し】 サブフォルダ移動**:
  現在のフォルダ直下にそのラベルと同名のフォルダが存在する場合、**そのサブフォルダへ瞬時に移動** して中身を読み込みます（フォルダがない場合は案内文が表示されます）。
- **【最上部の「..⤴」】 親フォルダ復帰**:
  サブフォルダに潜っているときにタップすると、直前の親フォルダへスムーズに戻ります。起点フォルダにいる時は薄いグレー表示になり無効化されます。

---

## 8. ファイル操作アクション（移動・リネーム・削除）

選択モード中、上部バーの **「〇枚選択」ボタンをタップ** すると、以下の実行メニューが表示されます。

- **連番→📁[ラベル]（連番移動）**:
  選択された画像を「[ラベル]」サブフォルダへ、`[ラベル]_[グループ番号]_[画像番号].[拡張子]`（例: `01犬_00_00.jpg`）の形式で連番リネームして移動します。
- **Rename→[ラベル]連番（連番リネーム）**:
  フォルダ移動は行わず、現在のフォルダ内でラベル付きの連番名にリネームします。
- **コピー / 移動**:
  保存先・移動先のフォルダピッカーが開き、選んだ場所へまとめて転送します。
- **削除**:
  選択画像を端末から削除します（必ず確認ダイアログが表示されます）。
- **[ラベル]📁一括変更**:
  フォルダ内の対象ファイル名に含まれるラベルを一括置換します。

---

## 9. よくある質問・トラブルシューティング

| 症状 / 疑問 | 原因と解決方法 |
|---|---|
| **画像を長押ししても選択モードに入らない** | 仕様変更です。上部の **カテゴリーラベル（`-[ラベル名]-`）をタップ** して選択モードを開始してください。長押し事故を防ぎ、快適にタップ選択できます。 |
| **選択した画像が暗くなるのはなぜ？** | 選択状態を分かりやすくするための「光度40%ダウン」仕様です。白いチェックアイコンが際立ち、選択されている画像が一目でわかります。 |
| **「〇枚選択」を長押ししたらどうなる？** | 選択モード（ピンク色や緑色）をキープしたまま、チェックだけが全解除されて「0枚選択」に戻ります。選択モード自体を終了したい時はカテゴリーラベルをタップしてください。 |
| **グループ表示中、選択が0枚なのにしおりが押せるのはなぜ？** | 最初に長押しした類似画像をアプリが記憶しているためです。選択を全解除しても、しおりを押せばいつでも元の類似画像へピョンピョン飛べます。 |
| **グループ表示からハッシュ画面に戻るには？** | 上部の **カテゴリーラベル（ライトグリーン `#4FFF94`）を長押し** してください。一瞬で元の★比較一覧に復帰します。 |
| **ハッシュ計算が長くて途中でやめたい** | 「★」をもう一度タップして解除するか、別のフォルダへ移動してください。裏の計算処理が即座に安全停止します。 |
| **誤って削除ボタンを押してしまわないか心配** | 削除や移動の実行前には、必ずAndroidシステムの確認ダイアログが表示されます。「許可」を押さない限り実行されません。 |

---

*最新更新: 2026年9月 / ImageViewOrganizer プロジェクト最新仕様準拠*
