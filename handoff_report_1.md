# ImageViewOrganizer 開発引継ぎリポート (handoff_report_1.md)

---

## １．アプリの概要と開発環境

### ■ アプリの目的
大量の画像ファイル（写真、イラスト、スクショ等）を、フォルダ単位で高速・直感的に閲覧・比較・重複検出・連番リネーム・サブフォルダ分類するための高機能Android画像管理アプリ。

### ■ 主な機能
1. **超高速ストリーミング画像閲覧:**
   - 大規模フォルダ（数千枚〜数万枚）でも遅延なく段階的に即時表示（SAF低レベルAPI高速クエリ）。
2. **直下プルダウンソート:**
   - 画面上部のソートボタン（≡）の真下に展開するプルダウンメニュー。
   - 新ソート「🏷️ グループ連番（枠色別）↑」により、リネーム済み画像のみを抽出し、グループごとに7色ローテーション枠線で色分け表示。
3. **★ 拡張選択モード（全体重複検索・類似画像検出）:**
   - 未選択状態から★ボタンで起動可能。
   - RGB5色パレット類似度判定 ＋ 知覚ハッシュ（pHash）スライダー（1〜20）による高精度類似グループ検出。
   - バックグラウンドでのハッシュ計算進捗表示（`ハッシュ計算中... 〇〇/〇〇枚 (〇%)`）。
   - 似た画像同士が隣り合うソート＆グループ別のカラー枠線表示。
4. **直感的フォルダ移動 ＆ ラベル選択（二刀流操作）:**
   - 上部のタグアイコン（🏷️）プルダウンから、**「通常タップで直下の該当フォルダへ瞬時移動」**、**「長押しでリネーム用ラベル変更」**を実現。
   - ラベル書式は `01犬`、`02猫`... とスペースなし2桁連番で完全統一。
5. **画像リネーム＆移動機能:**
   - `[ラベル]_[グループ2桁]_[画像連番2桁].[拡張子]`（例: `01犬_01_01.jpg`）の形式で安全に自動連番リネーム＆フォルダ移動。
6. **プレミアムな漆黒ダーク背景:**
   - 画像一覧の背景を完全な「黒（`Color.Black`）」に設定。写真の発色や7色カラー枠線が鮮明に浮かび上がる視認性を実現。

### ■ 開発環境・使用技術
- **開発者:** はづきさん（プログラミング勉強中）
- **対象プラットフォーム:** Android 12以降（API Level 31+）
- **言語:** Kotlin (Java 17 / JVM Target 17)
- **UIフレームワーク:** Jetpack Compose (Material3 + Material Icons Extended)
- **画像読み込み:** Coil Compose 2.6.0
- **アーキテクチャ:** MVVM (ViewModel + StateFlow + Kotlin Coroutines Flow)
- **ファイルアクセス:** Storage Access Framework (SAF) DocumentsContract

---

## ２．これまでに完了したこと・実装済みの機能

### 直近セッションで達成した実装内容
1. **「🏷️ グループ連番（枠色別）↑」ソートの新設:**
   - ソートメニューの最上部に配置。
   - フォルダ内から `_nn_mm` 書式のリネーム済み画像のみを抽出（未リネーム画像は非表示）。
   - 名前順（昇順: `↑`）でソートし、ラベル順 ➔ グループ順 ➔ 画像連番順に綺麗に整列。
2. **はづきさん厳選「見分けやすい7色ローテーション枠線」の導入:**
   - 隣り合うグループの境界が一目でわかるよう、明暗と色相が交互に配置された7色を定義：
     1. 🔴 レッド (`#E53935`)
     2. 🔵 藍紫 (`#5D63B0`)
     3. 🟠 オレンジ (`#FB8C00`)
     4. 🟡 若草色 (`#B5B877`)
     5. 🟣 パープル (`#8E24AA`)
     6. ⬛ ダークグレー (`#606060`)
     7. 🟢 翡翠グリーン (`#54AA70`)
   - 拡張選択（★）およびグループ連番ソートの両方で、グループごとに 1〜7 をローテーションして枠線色を適用。
3. **ギャラリー一覧の背景を「黒（`Color.Black`）」に変更:**
   - これまでの白系和紙背景から漆黒へ変更し、写真サムネイルと7色枠線のコントラストを大幅向上。
4. **ソートメニューの「直下プルダウン化」:**
   - 画面下からせり上がる旧ボトムシートを撤去し、上部「≡」ボタンの真下に `DropdownMenu` がスッと開くUIに変更。
5. **上部メニューアイコンのMaterial公式統一:**
   - ラベル選択ボタン: 仮のテキスト `□▷` ➔ 公式タグアイコン **`Icons.Default.Label`**（🏷️）
   - しおりボタン: 絵文字 `🔖` ➔ 公式リボン型アイコン **`Icons.Default.Bookmark`**
6. **ラベルメニューの二刀流化（タップでフォルダ移動 / 長押しでラベル変更）:**
   - `labels.txt` の読み込みを `01犬`、`02猫`... とスペースなしに統一。
   - 通常タップ時: 直下にそのフォルダが存在すれば瞬時にそのフォルダを開く（なければ案内通知）。
   - 長押し時: 作業ラベルを変更してスナックバーで通知。
   - メニュー上部に操作ヒント（上下2行: `💡 タップ→フォルダへ` / `　 長押し: ラベル変更`）を配置。

---

## ３．主要ファイルの配置と役割（プロジェクト構造）

```
app/src/main/
├── assets/
│   └── labels.txt                      # ラベルマスター（1 犬, 2 猫... 全23種類）
└── java/com/hazuki/imageorganizer/
    ├── data/
    │   ├── ImageItem.kt                # 画像メタデータ、SortOption enum（GROUP_SEQ_ASC新設）
    │   ├── ImageRepository.kt          # SAF高速クエリ、非同期ストリーミング、computeHashes
    │   ├── RenameMoveHelper.kt         # 連番リネーム計算、parseRenamedFileInfo(_nn_mm書式解析)
    │   └── RecentFoldersStore.kt       # フォルダ履歴管理
    ├── util/
    │   ├── ClassificationColorUtil.kt  # 【直前編集】はづきさん厳選7色ローテーションパレット管理
    │   ├── FileOperations.kt           # SAFドキュメント作成・コピー・削除・サブフォルダ検索
    │   ├── ImageGrouping.kt            # RGB5色パレット類似度 ＋ pHashによる重複グルーピング
    │   └── PerceptualHash.kt           # pHash（知覚ハッシュ）計算
    ├── viewmodel/
    │   ├── OrganizerUiState.kt         # UI状態管理（extensionGroupColors, hashMatchThreshold等）
    │   └── ImageOrganizerViewModel.kt  # 【直前編集】ソート、グループ色割り当て、サブフォルダ移動ロジック
    └── ui/
        ├── screens/
        │   ├── MainScreen.kt           # 【直前編集】メイン画面（背景Color.Black、TopBar接続）
        │   └── GroupDetailScreen.kt    # グループ個別閲覧画面
        ├── components/
        │   ├── TopBar.kt               # 【直前編集】上部バー（直下ソートプルダウン、各種アイコン）
        │   ├── LabelSelectDialog.kt    # 【直前編集】ラベルプルダウン（タップで移動、長押しで変更）
        │   ├── ImageGrid.kt            # グリッドサムネイル表示（カラー枠線描画）
        │   └── RenameActionMenu.kt     # 複数選択時のリネーム・移動アクションメニュー
        └── theme/
            ├── Color.kt                # 基本テーマカラー
            └── Theme.kt                # アプリテーマ設定
```

### 直前まで編集・作業していたファイルとその箇所
1. [`ClassificationColorUtil.kt`](file:///app/src/main/java/com/hazuki/imageorganizer/util/ClassificationColorUtil.kt#L29-L55)
   - `SEVEN_COLORS`（赤・藍紫・橙・若草・紫・ダークグレー・翡翠の7色）を定義し、ローテーションで枠線色を返却。
2. [`ImageItem.kt`](file:///app/src/main/java/com/hazuki/imageorganizer/data/ImageItem.kt#L45-L48)
   - `SortOption.GROUP_SEQ_ASC("🏷️ グループ連番（枠色別）↑")` のラベルと先頭配置。
3. [`ImageOrganizerViewModel.kt`](file:///app/src/main/java/com/hazuki/imageorganizer/viewmodel/ImageOrganizerViewModel.kt#L1510-L1605)
   - `loadLabels()`: スペースなし `01犬` 形式で読み込み。
   - `navigateToSubFolderIfExists(label)`: 直下のサブフォルダを検索して瞬時移動。
   - `setCurrentSelectedLabel(label, showFeedback)`: 長押し時のラベル変更と通知。
   - `sortImageList()` & `applyDisplayList()`: `GROUP_SEQ_ASC` の抽出・昇順ソート・7色枠線割り当て。
4. [`LabelSelectDialog.kt`](file:///app/src/main/java/com/hazuki/imageorganizer/ui/components/LabelSelectDialog.kt#L40-L130)
   - `Icons.Default.Label` のタグアイコン化。
   - 2行ヒント表示（`💡 タップ→フォルダへ` / `　 長押し: ラベル変更`）。
   - `combinedClickable` による通常タップ（フォルダ移動）と長押し（ラベル変更）の分岐。
5. [`TopBar.kt`](file:///app/src/main/java/com/hazuki/imageorganizer/ui/components/TopBar.kt#L245-L380)
   - ソートボタンの直下 `DropdownMenu` 化。
   - しおりボタンを `Icons.Default.Bookmark` に統一。
   - `onLabelSubFolderNavigate` を接続。
6. [`MainScreen.kt`](file:///app/src/main/java/com/hazuki/imageorganizer/ui/screens/MainScreen.kt#L173-L245)
   - 背景を `Color.Black`（漆黒）に変更。
   - `onSortOptionSelected` および `onLabelSubFolderNavigate` を ViewModel と接続。

---

## ４．開発上のルール・決定事項（絶対遵守）

1. **ユーザーキャラクターとトーン:**
   - ユーザー名は「はづきさん」（プログラミング勉強中）。
   - AIは専属の「超優秀なシニアエンジニア」として、常に優しく冷静で前向きな落ち着いた日本語で話す。
   - 難解な専門用語は身近な言葉で解説する。
2. **変更・提案の進め方:**
   - 勝手な大量変更は厳禁。修正や追加は必ず**1ステップずつ区切って提案し、ユーザーの許可を得てから実行**する。
   - コード変更前には「何のために、どこをどう変えるか」を日本語で分かりやすく説明する。
   - 1度のやり取りで変更する箇所は最大1〜2ファイルまで。
3. **過剰な日本語コメントの徹底:**
   - 作成・修正するコードには、処理の意味がわかる日本語コメントを「過剰なほど多め」に残す。
4. **命名・整理規則:**
   - ラベル形式: `01犬`、`02猫`...（2桁ゼロ埋め＋ラベル名、スペースなし）。
   - リネーム形式: `[ラベル]_[グループ2桁]_[画像連番2桁].[拡張子]`（例: `01犬_01_01.jpg`）。
   - 01〜99は数字2桁、100以降は英字2桁（AA〜ZZ）。
5. **エラー発生時のフロー:**
   - エラー原因の仮説を可能性の高い順に3つ提示 ➔ 具体的なネクストアクション1ステップを指示 ➔ 徹底伴走。

---

7. **下層フォルダ読み込み切り替え（「下層📁:OFF / ON」）機能の新設:**
   - 普段（デフォルト）は現在いるフォルダ直下のみを高速読み込み（起動時・★ボタン押下時ともに劇的に高速化）。
   - ★ボタン拡張メニュー内の「下層📁:OFF」ボタンをタップすると「下層📁:ON」になり、下層フォルダも含めた全体を即座に再読み込みして重複・類似判定を実行。
8. **プリセットボタンの文字変更:**
   - 「プリセット: OFF / A / B / C」 ➔ 「SET:0 / SET:A / SET:B / SET:C」にコンパクト化。
9. **ハッシュ値スライダーの感度・操作性大幅向上:**
   - スライダー範囲を 1〜20 ➔ **1〜17**（初期値10）に絞り込み、繊細な類似度調整を可能に。
   - スライダーの左右に **［−］** と **［＋］** の微調整ボタンを配置し、1刻みの快適なタップ調整を実現。
   - スライダーの横幅を130dpに最適化（以前の180dpからコンパクト化しつつ操作しやすい絶妙なサイズ感に調整）。
10. **「..⤴」一階層上へ戻るボタンの新設と自動グレーアウト制御:**
    - ラベル選択ダイアログ最上部の2行tipsを削除し、代わりに「..⤴」ボタンを配置。
    - 起点フォルダ（起動時や新規フォルダ選択時）ではグレーアウト（薄いグレー表示・タップ無効）。
    - ラベルタップで下層フォルダへ移動した時に通常表示になり、タップで直前の親フォルダへ瞬時に復帰。
    - 起点フォルダまで戻ると自動的に再度グレーアウト。

---

## ５．次にやりたいこと・現在発生している課題やエラー

### ■ 現在のステータス
- **ビルド・動作状態:** エラーなし。全機能の動作確認が完了し、非常に軽快かつ高精度に稼働中。

### ■ 次回以降の展望（はづきさんの構想メモ）
- 今回実装した「下層📁:OFF / ON」やハッシュ値微調整（＋−ボタン）を実際に使い込んでみて、操作感の微調整や新たな機能追加のアイデアを検討予定。

---

## ６．次のAI（新しいチャット）への指示・Initial Command

以下の枠内の文章を、新しいチャットの一通目にそのまま貼り付けて送信してください。

```markdown
専属シニアエンジニアさん、お疲れ様です！はづきです。
Androidアプリ「ImageViewOrganizer」の開発の続きをお願いします！

添付したプロジェクト全体のZIPファイルと、ルート直下の引継ぎレポート `handoff_report_1.md`、および `GEMINI.md` のルールを確認してください。

現在、以下の機能が正常に完成し、ビルド・動作確認まで完了しています：
1. 「🏷️ グループ連番（枠色別）↑」ソート（_nn_mm形式のみ抽出、名前昇順整列、7色枠線表示）
2. はづき厳選7色ローテーション枠線（赤・藍紫・橙・若草・紫・ダークグレー・翡翠）
3. ギャラリー一覧の背景を「黒（Color.Black）」に変更
4. 上部ソートボタン（≡）の直下プルダウンメニュー化
5. 上部アイコンの統一（ラベル=タグアイコン Icons.Default.Label、しおり=Icons.Default.Bookmark）
6. ラベルメニューの二刀流化（通常タップで直下フォルダ移動、長押しでラベル変更、「..⤴」で親フォルダ復帰）
7. 下層フォルダ読み込み切り替え（「下層📁:OFF / ON」ボタン、デフォルトは直下のみ高速読込）
8. プリセット表記を「SET:0 / SET:A / SET:B / SET:C」に変更
9. ハッシュ値スライダー（1〜17、左右の［−］［＋］微調整ボタン付き、幅130dpに最適化）
10. ラベルマスター読み込みの正規化（先頭の連番00〜22の二重付与を解消し、本来のラベル名のみを美しく表示）
11. 「📁一括変更」の完全動作（フォルダ内のファイル連番置換に加え、フォルダ本体も新ラベル名に自動リネーム）
12. リネーム時の「(1)」枝番完全救出＆二重付与防止（半角・全角の枝番を自動吸収して正規復元、同名時のOS誤作動防止）
13. 親Tree権限（マスターキー）の永続保持（サブフォルダ移動やリネーム時に権限再確認や「画像が見つかりませんでした」が挟まる問題を完全解決）

まずは「引き継ぎ完了しました！」と落ち着いた優しいトーンでご挨拶いただき、次の開発・改善に向けた準備が整ったことをお知らせください。よろしくお願いいたします！
```

