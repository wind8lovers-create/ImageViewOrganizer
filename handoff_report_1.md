# 開発引継ぎ用レポート (handoff_report_1.md)

---

## １．アプリの概要と開発環境

### アプリの目的・主な機能
* **アプリ名:** ImageViewOrganizer
* **目的:** スマートフォン内（ローカル・SDカード・外部ストレージ）やZIP書庫内の大量の画像を、AI認識カテゴリ・色味・類似度（ハッシュ値・RGB5色）・縦横比などで素早く分類・整理・一括連番リネーム・フォルダ移動するための多機能画像オーガナイザー。
* **主な機能:**
  1. **高速画像ギャラリー・フルスクリーンビューア:** 大量画像の高速グリッド表示（小/大切り替え）、全画面プレビュー、スライドショー再生。
  2. **リネーム＆フォルダ移動システム:**
     * `[ラベル名]_[グループ番号2桁]_[画像連番2桁].[拡張子]`（例: `01犬_01_01.jpg`〜`01犬_01_05.jpg`）の統一命名規則。
     * ルートフォルダおよび直下ラベルフォルダを統合スキャンし、重複しないグループ番号の自動採番。
     * 「連番→📁[ラベル]」（フォルダ作成＋コピー＆元ファイル削除による完全移動）。
     * 「Rename→[ラベル]連番」（その場での連番リネーム）。
     * 途中エラー時の自動ロールバック（元ファイル保護）と明確なエラー通知。
  3. **高機能上部メニュー（TopBar）:**
     * 親フォルダを除去した現在のフォルダ名（フルネーム）表示。
     * 📁フォルダ選択、📦ZIP書庫展開、🕐フォルダ履歴ドロップダウン、≡ソート切り替え。
     * 🔖しおりボタン（選択中画像の順次巡回ジャンプ＆先頭戻り）。
     * 全幅横スクロール対応（スマホの画面幅でも全ボタンが快適に操作可能）。
  4. **拡張選択パネル（「★」アイコン）:**
     * 彩度・明度スライダー（180dpロングストローク仕様）、プリセットA/B/C切り替え。
     * 画像ハッシュ値、縦横比、RGB5色類似度スライダーによる高度な画像絞り込み。

### 対象OS / 開発環境
* **対象OS:** Android 12以降（API Level 31+、ターゲットAPI 34/35）
* **開発環境:** Android Studio / Windows / Google Antigravity
* **言語:** Kotlin (100%)
* **UIフレームワーク:** Jetpack Compose (Material 3)
* **画像読み込み:** Coil (ImageLoader)
* **非同期処理:** Kotlin Coroutines & Flow
* **ストレージ操作:** Storage Access Framework (SAF: DocumentsContract) & MediaStore

---

## ２．これまでに完了したこと・実装済みの機能

1. **見本コード（`Guide_code` 内）の完全統合**
   * `RenameMoveHelper.kt` を拡張し、SAF環境（DocumentsContract）でも安全に動作するファイル名判定・グループ採番ロジックを実装。
2. **リネーム規則の「01スタート（1始まり）」への統一**
   * グループ番号（`01`〜`99`, `AA`〜`ZZ`）および画像連番（`01`〜`99`, `AA`〜`ZZ`）をすべて 01 スタートに統一。5枚選択なら末尾 `05` で終わる直感的な命名規則を確立。
3. **ルート＆直下サブフォルダの「統合グループ判定」**
   * ルートフォルダと直下にある同じラベルのフォルダ（例: `01犬`）の両方を合算して既存の最大グループ番号をスキャン。
   * ルートでリネームしようが、サブフォルダに移動しようが、常に全体の続き番号（例: `06` の次は `07`）から採番され、ファイル名重複を完全防止。
4. **SAF移動の安定化と自動ロールバック**
   * Androidの不安定な `moveDocument` を廃止し、見本コードの思想である「新フォルダへストリームコピー ➔ 成功確認後に元ファイル削除」方式に変更。
   * コピー途中でエラーが発生した場合は、作成途中の新ファイルを全件削除して元の状態に戻し、元ファイルは1枚も消さずにスナックバーでエラーを明示通知。
5. **ラベル選択状態の永続維持と初期化**
   * アプリ起動時に先頭ラベル（例: `01 犬`）を自動設定し、nullや空文字を防止。
   * 画像選択時・クリア時・コマンド実行後も選んだラベルをずっと維持。
   * ラベル名（`-01 犬-`）とリスト展開ボタン（`□▷`）を独立したボタンとして配置。
6. **上部メニュー（TopBar）の全面刷新**
   * **横スクロール対応:** `horizontalScroll` を導入し、画面幅の狭い端末でも全ボタン（左・中央・右）が隠れずスライド操作可能。
   * **フォルダ名:** 親フォルダパスを除去し、現在開いているフォルダのフルネーム（`未整理`、`01犬` など）を省略なしで表示。
   * **「🕐」フォルダ履歴:** 過去に開いたフォルダ一覧がドロップダウンメニューでパッと開き、1タップでフォルダ切り替えが可能。
   * **「🔖」しおり機能:** 画像選択時にアクティブ化。タップで選択画像へ順次スクロールジャンプ、長押しで先頭選択画像へジャンプ。
   * **「★」拡張選択パネルの完全復元＆大型化:**
     * 彩度・明度・RGB5色のスライダー幅を `180dp`（初期比1.6倍）に広げ、指先での微調整が快適に行えるように改善。
     * ハッシュ値・縦横比スイッチ、プリセットA/B/Cボタンも完全動作。

---

## ３．主要ファイルの配置と役割（プロジェクト構造）

```
app/src/main/java/com/hazuki/imageorganizer/
├── data/
│   ├── ImageItem.kt             # 画像のメタデータ（ID, Uri, 表示名, 日時, サイズ, RGB/HSV色情報等）
│   ├── RecentEntry.kt           # フォルダ履歴エントリ（URI, 表示ラベル, 最終アクセス日時）
│   ├── RenameMoveHelper.kt      # リネーム・移動ロジック（775通り変換、01スタート、グループ採番）★
│   └── SortOption.kt            # ソート種別（名前順, 日時順, サイズ順, 類似度順等）
├── ui/
│   ├── components/
│   │   ├── TopBar.kt            # 上部メニューバー（横スクロール、フォルダ名、履歴、ラベル、しおり、拡張パネル）★直前編集
│   │   ├── BottomBar.kt         # 旧ボトムバー（※現在はScaffoldから外され非表示・廃止済み）
│   │   ├── LabelSelectDialog.kt # 「□▷」ボタンによる23種類ラベル選択ドロップダウン
│   │   └── FullscreenViewer.kt  # 全画面画像プレビュー・スライドショー
│   ├── screens/
│   │   └── MainScreen.kt        # メイン画面（Scaffold, TopBar接続, フォルダ/ZIPランチャー, グリッド表示）★
│   └── theme/                   # カラーパレット（FujiPrimary, FujiPrimaryDarkなど）
├── util/
│   ├── FileOperations.kt        # SAF操作ユーティリティ（サブフォルダ検索/作成、子ファイル名クエリ）★
│   └── ImageGrouping.kt         # 色味・ハッシュ値による画像グルーピング計算
└── viewmodel/
    ├── ImageOrganizerViewModel.kt # アプリの全状態管理、SAFリネーム・移動・コピー・ロールバック実行 ★
    └── OrganizerUiState.kt        # 画面状態データクラス
```

### 直前まで編集・作業していたファイルとその箇所
1. **`app/src/main/java/com/hazuki/imageorganizer/ui/components/TopBar.kt`**
   * **150〜340行目:** `horizontalScroll` の導入、フォルダ名フルネーム表示、フォルダ履歴（`DropdownMenu`）の実装、しおりボタン（`isEnabled = selectedCount > 0`）の有効化。
   * **600〜760行目:** `ExtensionSelectionPanel` の完全復元、スライダー幅を `180dp` に拡大、非線形変換関数（`mapSliderToThreshold` / `mapThresholdToSlider` / `lerp`）の実装。
2. **`app/src/main/java/com/hazuki/imageorganizer/viewmodel/ImageOrganizerViewModel.kt`**
   * **78〜83行目:** 起動時の初期ラベル自動設定（null/空文字防止）。
   * **580〜650行目:** 選択・クリア時に `currentSelectedLabel` を消さず永続維持。
   * **1520〜1760行目:** `executeRenameMove`（SAFストリームコピー＆削除、ロールバック）および `executeRenameOnly` の実装。カレントフォルダと直下ラベルフォルダを合算した統合グループ採番（`findNextGroupIndexFromNames`）。
3. **`app/src/main/java/com/hazuki/imageorganizer/util/FileOperations.kt`**
   * **327〜370行目:** `findSubFolderUriSaf`, `getOrCreateSubFolder`, `queryFolderChildNamesSaf` の追加。
4. **`app/src/main/java/com/hazuki/imageorganizer/data/RenameMoveHelper.kt`**
   * **40〜125行目:** `toSeqCode`, `fromSeqCode`, `findNextGroupIndexFromNames` を 01 スタート（1〜99: 数字2桁, 100〜775: AA〜ZZ）に統一。

---

## ４．開発上のルール・決定事項（重要）

* **ユーザー名:** はづきさん（プログラミング初心者）。
* **AIのキャラクター:** 専属・超優秀なシニアエンジニア。常に優しく冷静で前向き、丁寧で落ち着いた日本語。
* **行動規範（GEMINI.md 絶対遵守ルール）:**
  1. **意図の組み取り最優先:** メニュー配置、ボタン、スライダー、命名規則など、ユーザーが提示した意図を最大限組み取ること。「やらないで」と言われたことは絶対にやらない。
  2. **勝手な大量変更の禁止:** 修正・追加は必ず「1ステップずつ（最大1〜2ファイルまで）」に区切り、何のためにどこをどう変えるか日本語で分かりやすく説明し、許可を得てから実行する。
  3. **過剰な日本語コメントの徹底:** 作成・修正するコードには、初心者が読んでも処理の意味が分かる日本語コメントを過剰なほど多めに残す。
  4. **誠実さの徹底:** 実装していないものをあたかも実装している風に振る舞ったり、不都合な点を黙ったりしない。
  5. **エラー発生時のフロー:** ①仮説を可能性の高い順に3つ提示 ➔ ②具体的なネクストアクション（1ステップ）を指示 ➔ ③徹底伴走。
* **命名・配置ルール:**
  * リネーム形式: `[ラベル]_[グループ2桁]_[連番2桁].[拡張子]`（両方とも 01 スタート）。
  * グループ採番は「カレントフォルダ」と「直下の同名ラベルフォルダ」の両方をスキャンして決定。
  * ボトムバーは廃止済み。機能は上部メニューに一本化。

---

## ５．次にやりたいこと・現在発生している課題やアイデア

* **現状のステータス:**
  * 直近の課題（ラベル状態維持、リネーム連番規則、フォルダ移動、しおり機能、フォルダ履歴、拡張選択パネルの復元・スライダー拡大）は**すべて完了し、完全に正常動作を確認済み**。
* **次スレッドでの検討事項・今後の構想:**
  * はづきさんが実機で使い込んだ後のフィードバックや改善要望への対応。
  * 未使用コードとなった旧 `BottomBar.kt` のクリーンアップ整理（必要に応じて）。
  * 新しい機能の追加や、UI/UXの更なるブラッシュアップ。

---

## ６．次のAI（新しいチャット）への指示・Initial Command

新しいスレッドを開いた際は、以下のメッセージを1通目にそのまま送信してください。

```markdown
添付したZIPファイル（プロジェクト一式）と、ルートフォルダにある「handoff_report_1.md」を読み込んで、続きの作業を開始してください。

私はプログラミング初心者の「はづき」です。
プロジェクトルートにある「GEMINI.md」に私の開発環境や専属シニアエンジニアとしての行動規範（日本語での思考・出力、勝手な大量変更禁止、1ステップずつの提案と許可、過剰な日本語コメントの徹底など）が定義されていますので、必ず熟読してルールを厳守してください。

まずは現在のプロジェクト状況を把握した旨と、今後の改善点や新しい構想について伺う準備ができたことを、優しく前向きな日本語でお知らせください。
```
